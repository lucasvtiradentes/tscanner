---
source: crates/core/tests/spec_tests.rs
assertion_line: 38
expression: snapshot
---
# Input
```ts
const x = a && a.b;

const y = foo && foo.bar;

const z = obj && obj.property && obj.property.nested;
```

# Diagnostics
```
invalid.ts:1:12 prefer-optional-chain ━━━━━━━━━━━━━━━━━━━━

  ! Use optional chaining (?.) instead of logical AND (&&) for null checks.

  > 1 │ const x = a && a.b;
    2 │ 

```

```
invalid.ts:3:12 prefer-optional-chain ━━━━━━━━━━━━━━━━━━━━

  ! Use optional chaining (?.) instead of logical AND (&&) for null checks.

    2 │ 
  > 3 │ const y = foo && foo.bar;
    4 │ 

```

```
invalid.ts:5:12 prefer-optional-chain ━━━━━━━━━━━━━━━━━━━━

  ! Use optional chaining (?.) instead of logical AND (&&) for null checks.

    4 │ 
  > 5 │ const z = obj && obj.property && obj.property.nested;

```
