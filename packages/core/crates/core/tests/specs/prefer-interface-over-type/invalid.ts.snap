---
source: crates/core/tests/spec_tests.rs
expression: snapshot
---
# Input
```ts
type User = {
  name: string;
  age: number;
};

type Config = {
  timeout: number;
};
```

# Diagnostics
```
invalid.ts:1:2 prefer-interface-over-type ━━━━━━━━━━━━━━━━━━━━

  ! Use 'interface' instead of 'type' for object types

  > 1 │ type User = {
    2 │   name: string;

```

```
invalid.ts:6:2 prefer-interface-over-type ━━━━━━━━━━━━━━━━━━━━

  ! Use 'interface' instead of 'type' for object types

    5 │ 
  > 6 │ type Config = {
    7 │   timeout: number;

```
