---
"tscanner": major
"tscanner-vscode": major
---

Replace the old `aiRules` prompt map with provider-style markdown rule sources. Configure AI rules as source entries that point to Cursor, Claude, or generic markdown files and folders.
